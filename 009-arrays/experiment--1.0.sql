-- complain if script is sourced in psql, rather than via CREATE EXTENSION
\echo Use "CREATE EXTENSION experiment" to load this file. \quit

CREATE OR REPLACE FUNCTION experiment_sum(arr INT[]) RETURNS INT
AS 'MODULE_PATHNAME', 'experiment_sum' LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT;

CREATE OR REPLACE FUNCTION experiment_max(arr ANYARRAY) RETURNS ANYELEMENT
AS 'MODULE_PATHNAME', 'experiment_max' LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT;

CREATE OR REPLACE FUNCTION experiment_repeat(val ANYELEMENT, count INT) RETURNS ANYARRAY
AS 'MODULE_PATHNAME', 'experiment_repeat' LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT;
